# OASIS v9.3.0 적용 방법

1. 현재 프로젝트 버전이 `v9.2.0`인지 확인합니다.
2. ZIP 안의 모든 파일과 `payload` 폴더를 프로젝트 최상위 폴더에 덮어씁니다.
3. `RUN_UPDATE_v9.3.0.bat`를 실행합니다.
4. `UPDATE_OK`와 `VERSION=v9.3.0`이 표시되는지 확인합니다.
5. Railway 배포 후 `AI 컨설팅 코파일럿 → 상담 플레이북`에서 기능을 확인합니다.

업데이트 전 파일은 `backup/before_v9.3.0_날짜_시간` 폴더에 자동 보관됩니다.
