# OASIS CRM v9.8.9 변경이력

## Added

- 전사 공통 업체 식별키 `company_uid` 및 강한 식별값 우선순위
- 검색 후보의 canonical UID 일괄 resolver와 안전한 source identity 채택
- 업체별 전사 배정·상태·연락집계 테이블
- 사용자별 비공개 메모, 구조화된 연락이력, 조회이력, 감사로그, 기존 담당 충돌 테이블
- `oasis_claim_and_save_company_sales_assignment` 원자적 RPC
  - 임시배정과 기존 `oasis_prospect_companies` 저장을 한 트랜잭션에서 처리
  - 원천키·UID 불일치 차단
  - 동시 저장, 24시간 만료, 사용자별 30건 한도 처리
- 검색 결과의 `내 영업DB에 담고 연락결과 기록` 빠른 흐름
- 관리자 담당 변경·강제 회수·재활성화·영구 제외·사용자 한도 관리
- 관리자 서버 집계 RPC와 `LIMIT/OFFSET` 페이지 목록
- 현재 페이지의 기존 다중 담당 충돌 확인·해결 UI
- 배정·충돌·만료·연락·관리자 변경 감사로그
- 신규 테이블 RLS와 service_role 전용 서버 RPC 권한
- 연락처 보강 작업의 카카오·네이버 제공자 단계 원자적 선점 조건

## Changed

- 신규 영업후보 저장 경로를 구식 “선배정 후 별도 REST 저장 및 보상 해제”에서 업체별 단일 원자적 RPC로 교체
- 검색·상세 조회는 조회이력만 남기고 배정하지 않도록 명확히 분리
- 저장 성공·이미 내 업체·다른 담당자 충돌·한도초과를 업체별 결과로 표시
- 연락결과 저장 시 담당 확정, 최초·최근 연락일, 연락횟수, 다음 연락일을 함께 갱신
- 관리자 전사 통계를 전체 행 클라이언트 합산 대신 서버 집계로 변경
- 관리자 업체목록을 100·200·500건 페이지 단위 렌더링으로 변경
- 기존 상태값을 신규 상태 체계와 호환되도록 매핑
- 일반 사용자의 응답 필드를 허용목록으로 정제해 타 사용자 정보 노출 차단

## Migration

- 기존 업체 ID·원천행·검색이력·메모·CRM 데이터 보존
- 기존 사용자별 메모를 `company_uid + user_id` 기준으로 멱등 이관
- 단일 담당 기존 저장업체를 `legacy_hold`로 보호해 최초 이관 직후 자동 만료 방지
- 복수 사용자 저장업체를 삭제하지 않고 `oasis_company_assignment_conflicts`로 이관
- `oasis_consultation_journals`의 실제 상담이력을 구조화된 연락로그로 이관
  - 상담일지별 고유 원천키로 재실행 중복 방지
  - 단일 담당자의 담당상태·연락횟수·최초/최근 연락일 복원
  - 저장 담당자와 상담 담당자가 다르면 관리자 해결 충돌로 보존
- 기존 번호오류 행에 정규화된 전화번호 기준 지문 설정

## Fixed

- A와 B가 동시에 같은 업체를 저장할 때 중복 배정될 수 있는 경쟁 조건
- 배정 성공 후 기존 영업DB 저장 실패로 두 상태가 어긋날 수 있는 문제
- 업체명만 또는 변경 가능한 전화표기 때문에 업체 식별이 흔들리는 문제
- 번호 형식만 바뀌어 번호오류 업체가 잘못 재활성화될 수 있는 문제
- source identity와 강한 식별값이 충돌한 후보가 잘못 저장·배정될 수 있는 문제
- `oasis_employment_contacts`에서 새 전화번호가 확보돼도 번호오류 지문에 반영되지 않던 문제
- 대량 관리자 목록을 한 번에 불러오고 화면에서 집계하던 성능 문제
- 기존 상담일지가 담당 확정과 연락횟수에 반영되지 않던 문제
- 기존 다중 담당 충돌을 화면에서 명확히 확인·해결하기 어려운 문제
- 네이버 연락처 조회의 일시 오류 건이 완료 처리되어 재시도 대기열에서 빠질 수 있던 문제

## Preserved

- 기존 DB발굴 검색조건과 검색 제외 흐름
- 사용자별 조회이력, 저장된 영업후보, 메모
- 고객관리, 기업등록, 기업컨설팅, 경정청구, AI 코파일럿
- 기존 Supabase 원천 데이터와 업체 ID
- 기존 매일 연락처 보강 작업의 카카오 1차·네이버 2차 순서와 상태값

## Validation

- 패치 단위·모의통합·정적 계약·연락처 보강 회귀 테스트: `71 passed`
- 전체 프로젝트 회귀시험: `423 passed, 1 skipped, 1 failed`
- 기존 `origin/main`에도 존재하는 `claim_correction_center.py` 재실행 개수 기대값 불일치 1건은 이번 패치 범위 밖이며, 해당 파일은 변경·포함하지 않음
- 운영/스테이징 Supabase 실동시성·RLS·시간 이동 검증은 배포 전 별도 수행 필요
