# OASIS CRM v9.12.0 중앙 업무함과 후속조치 자동화

## 2026-08-05

- 담당자별 자동업무, 고객 CRM 후속일정, 영업 재연락 일정을 한 화면에서 확인하는 중앙 업무함을 추가했다.
- 자동업무 시작·완료·연기는 낙관적 버전 잠금과 PII 없는 append-only 이벤트 이력을 사용하며, 응답 유실 재시도도 멱등 처리한다.
- 영업 후속일정은 기존 상태 변경형 목록 RPC를 호출하지 않고, 담당자 범위의 읽기 전용 키셋 RPC와 전용 인덱스로 조회한다.
- CRM 클라우드 복원 실패는 원시 예외를 노출하지 않고 고정 경고와 degraded 상태로 표시한다.
- 신규 RPC 5개는 서비스 역할 전용이며, 업무·이벤트 표는 FORCE RLS와 최소 권한을 유지한다.
- 격리 Supabase에서 동일 SQL 2회 적용, 동일 시각 영업 1,001건 키셋 경계, 원천행 불변 해시, 관련 4개 표 백업·복구 지문을 검증했다.
- 전체 회귀 `877 passed, 1 skipped, 87 subtests passed`, 보호 기능 회귀 `407 passed, 1 skipped, 54 subtests passed`, Privacy Guard를 통과했다.
- 운영 적용 전후 핵심 11개 표·보호 4개 표·대기열 10개 표의 건수와 고객 ID·`company_uid`·상태 지문이 모두 동일했다.
- GitHub PR #71을 `main`에 반영했고, 기능 커밋 기준 Railway 주 앱·경정청구 인증 게이트웨이·전화 일간수집·자격 월간수집 네 서비스의 배포 성공을 확인했다.

---

# OASIS CRM v9.11.0 고객정보 통합

## 2026-08-04

- 로컬 누적 고객DB와 Supabase 고객을 정확한 10자리 사업자등록번호로만 무손실 통합한다.
- 기존 고객 UUID와 영업 `company_uid`를 바꾸지 않는 소유자 범위 교차 연결을 추가했다.
- CRM·재무·등기·매칭설정·고객이력·상담일지·휴지통·주가평가에 nullable 고객 FK, 인덱스와 미래 행 자동 연결 트리거를 추가했다.
- 고객 저장·조회는 서비스 역할 전용 RPC를 우선 사용하고, 실패 시 원본을 덮어쓰지 않는 RPC 재시도 대기열에 보존한다.
- 고객 이관의 식별 불가·대기·실패 건을 완료로 오인하지 않고 오류 원문을 이력에 저장하지 않는다.
- 기존 기능, 고객 원본, 고객 ID, `company_uid`, 전화수집·원천DB 상태, 경정청구/Tilko 경로는 삭제하거나 축소하지 않았다.
- 격리 Supabase 동일 SQL 2회 적용, 논리 백업·복원, 운영 전후 건수·지문 비교, 전체 회귀 `652 passed, 1 skipped, 22 subtests passed`, Privacy Guard를 통과했다.
- 운영 Supabase 마이그레이션, GitHub `main`, Railway 연관 서비스 4개 배포를 모두 확인해 2단계를 완료 처리했다.

---

# OASIS v8.5.3 안정화 패치 — 누적 이력 보존형

## 기능 수정

- Railway/Streamlit 환경의 한글 PDF 폰트 경로 오류 완화
- 크레탑 고객등록과 정책자금 키워드 저장 단계 분리
- 정책자금 키워드 정규화 및 안전 저장
- 주요 오류 자동 로그 기록
- 외부 라이브러리 Import 없이 문법검사만 수행

## 업데이트 엔진 수정

- 이전 패치폴더를 삭제하지 않음
- 이전 패치 ZIP을 삭제하지 않음
- 이전 BAT 및 업데이트 엔진을 삭제하지 않음
- 현재 패치폴더를 업데이트 후에도 보존
- 명시된 프로젝트 코드 파일만 백업 후 덮어쓰기
- 실패 시 이번 패치가 관리하는 대상 파일만 자동 복원
