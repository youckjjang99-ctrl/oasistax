# OASIS CRM v9.13.0 저장 일관성·조회 최적화

## 2026-09-09 · 로컬 검증 완료 / 운영 미적용

- 최신 origin/main `4db662c` 기준 별도 `codex/crm-consistency-20260909` worktree에서 작업했다. 기존 작업 폴더와 미커밋 변경을 보존했다.
- 요청한 순서대로 동시 저장 충돌 방지 → 기기 간 동기화 → 연락상태 조회 정확성 → 반복 조회 최적화를 구현하고 단계별 테스트 후 다음 단계로 진행했다.
- 로컬 CRM은 파일 잠금·원자적 교체·고객 revision 검사로 보호한다. 오래된 편집은 초안으로 보존하고 손상 JSON을 빈 데이터로 덮어쓰지 않는다. 상담 타임라인 저장 80건 제한을 제거했다.
- 클라우드 CRM은 트랜잭션 잠금·버전 비교·멱등 요청 RPC로 저장한다. 기존 ID와 원천행을 유지하며 충돌·미전송 변경·동일한 내용의 별도 이력도 보존한다.
- 화면 재실행 시 60초 간격의 소유자별 클라우드 재확인과 수동 최신본 불러오기를 지원한다. 편집 중 입력·프로필 확장값·로컬 이력을 보관한 뒤 최신본을 채택한다. 실시간 background push는 아니다.
- 전역 연락기록 최근 1,000건 기반 요약을 업체별 현재 배정 요약 RPC로 교체했다. 재배정 이전 담당자·다른 작성자 기록이 일반 사용자에게 섞이지 않으며 목록과 집계가 같은 기준을 사용한다.
- 숫자 요약·업무함에 사용자·역할·변경 세대를 구분하는 TTL 20초 캐시를 적용했다. 실제 오류·부분 응답은 정상 0건으로 캐시하지 않으며, 화면 밖의 저장 성공도 무효화한다.
- 재무·등기 본문 캐시는 매번 소유자·수정시각을 서버에서 재확인한다. legacy 보조 식별 결과는 캐시하지 않는다. 합성 반복 10회 조회에서 전체 본문 1회/작은 메타데이터 9회로 확인했다.
- 일반 저장은 해당 사용자 캐시만 갱신하고, 관리자 이관·회수 등은 별도 안전 무효화를 수행한다. 다른 Railway 인스턴스의 일부 요약에는 최대 20초 지연이 남는다. 상담 원문·상세 권한은 캐시로 판정하지 않는다.
- 전체 pytest `1202 passed, 92 subtests passed`. 격리 PostgreSQL 17.10에서 저장 15개·연락상태 14개 시나리오와 SQL 2회 적용, 합성 원본 보존을 검증했다.
- 기존 Daum 테스트의 오래된 마이그레이션 파일명 참조, UI 테스트의 전역 조회 횟수 기대값, Windows spawn 테스트의 Streamlit 임시 main 경로 오염을 수정했다. 제품 기능 축소나 테스트 생략은 없다.
- SQL 2개, 검증 BAT, 변경 파일 목록, 운영 적용·롤백 문서와 덮어쓰기 패치를 제공한다. 운영 Supabase 적용·Git 커밋/push·Railway 배포·유료 API 호출은 하지 않았다.
- 자세한 범위·제한·권한·사용법은 `docs/crm-consistency-v9.13.0.md`에 기록했다. 경정청구/Tilko·전화번호 수집·원천 DB 로직은 유지했다.

---

# OASIS CRM v9.12.1 기업등록·이미지 PDF 인식 개선

## 2026-09-09 · 로컬 검증 완료 / 운영 배포 전

- 최신 origin/main `51cb26a` 기준 별도 작업 브랜치에서 수정했으며 기존 작업 폴더의 변경사항을 보존했다.
- 텍스트 없는 크레탑 PDF는 페이지별 한국어 OCR로 읽고, 완전히 빈 추출 결과는 성공으로 처리하지 않는다.
- 업체명·대표자·사업자번호·법인번호·주소·설립일·업종·기업유형 추출을 보완했다. 없는 재무·인원·인증값은 추정하지 않는다.
- 대상 기업의 재무자료가 없을 때 업종 비교표의 타 기업 금액을 가져오는 경로를 차단했다.
- 기업등록을 ‘새 기업 등록’ / ‘등록기업에 자료 추가’로 분리하고 선택한 기능만 실행한다. 기본정보 확인 form, 선택형 상세·매칭설정, 명시적인 저장 단계를 제공한다.
- 업로드 교체 시 이전 결과·수정값·설정을 초기화한다. 분석만으로 고객/스냅샷을 갱신하지 않으며, 기존 기업의 갱신은 확인 후 수행한다.
- 다른 정상 사업자번호를 가진 동명 기업을 갱신하지 않도록 보완했다. 사용자가 확인한 기본정보 수정값은 로컬에도 일관되게 반영한다.
- 재무가 없는 보고서는 기존 재무 스냅샷을 대체하지 않으며, 미확인 인증은 기존 고객 값을 유지한다.
- 클라우드 동기화 실패를 성공으로 표시하지 않는다. 매칭설정은 명시적으로 선택한 경우에만 저장하며 기존 추천정보를 유지한다.
- 새 파서·UI 테스트 47개와 기존 관련 회귀 67개, 총 114개 통과. 실제 첨부 PDF 17쪽을 subprocess 경로로 검증했다.
- Supabase 스키마/운영 데이터, 경정청구·Tilko·전화번호 수집 로직은 변경하지 않았다. Git push 및 Railway 배포는 수행하지 않았다.
- 브라우저 도구의 URL 확인 실패로 시각 검증은 중단했다. 운영 반영 전 PC/모바일 최종 화면 확인이 필요하다.

---

# OASIS CRM v9.12.0 중앙 업무함과 후속조치 자동화

## 2026-08-05

- 담당자별 자동업무, 고객 CRM 후속일정, 영업 재연락 일정을 한 화면에서 확인하는 중앙 업무함을 추가했다.
- 자동업무 시작·완료·연기는 낙관적 버전 잠금과 PII 없는 append-only 이벤트 이력을 사용하며, 응답 유실 재시도도 멱등 처리한다.
- 영업 후속일정은 기존 상태 변경형 목록 RPC를 호출하지 않고, 담당자 범위의 읽기 전용 키셋 RPC와 전용 인덱스로 조회한다.
- CRM 클라우드 복원 실패는 원시 예외를 노출하지 않고 고정 경고와 degraded 상태로 표시한다.
- 신규 RPC 5개는 서비스 역할 전용이며, 업무·이벤트 표는 FORCE RLS와 최소 권한을 유지한다.
- 격리 Supabase에서 동일 SQL 2회 적용, 동일 시각 영업 1,001건 키셋 경계, 원천행 불변 해시, 관련 4개 표 백업·복구 지문을 검증했다.
- 전체 회귀 `877 passed, 1 skipped, 87 subtests passed`, 보호 기능 회귀 `407 passed, 1 skipped, 54 subtests passed`, Privacy Guard를 통과했다.
- 운영 적용 전후 핵심 11개 표·보호 4개 표·대기열 10개 표의 건수와 고객 ID·`company_uid`·상태 지문이 모두 동일했다.
- GitHub PR #71을 `main`에 반영했고, 기능 커밋 기준 Railway 주 앱·경정청구 인증 게이트웨이·전화 일간수집·자격 월간수집 네 서비스의 배포 성공을 확인했다.

---

# OASIS CRM v9.11.0 고객정보 통합

## 2026-08-04

- 로컬 누적 고객DB와 Supabase 고객을 정확한 10자리 사업자등록번호로만 무손실 통합한다.
- 기존 고객 UUID와 영업 `company_uid`를 바꾸지 않는 소유자 범위 교차 연결을 추가했다.
- CRM·재무·등기·매칭설정·고객이력·상담일지·휴지통·주가평가에 nullable 고객 FK, 인덱스와 미래 행 자동 연결 트리거를 추가했다.
- 고객 저장·조회는 서비스 역할 전용 RPC를 우선 사용하고, 실패 시 원본을 덮어쓰지 않는 RPC 재시도 대기열에 보존한다.
- 고객 이관의 식별 불가·대기·실패 건을 완료로 오인하지 않고 오류 원문을 이력에 저장하지 않는다.
- 기존 기능, 고객 원본, 고객 ID, `company_uid`, 전화수집·원천DB 상태, 경정청구/Tilko 경로는 삭제하거나 축소하지 않았다.
- 격리 Supabase 동일 SQL 2회 적용, 논리 백업·복원, 운영 전후 건수·지문 비교, 전체 회귀 `652 passed, 1 skipped, 22 subtests passed`, Privacy Guard를 통과했다.
- 운영 Supabase 마이그레이션, GitHub `main`, Railway 연관 서비스 4개 배포를 모두 확인해 2단계를 완료 처리했다.

---

# OASIS v8.5.3 안정화 패치 — 누적 이력 보존형

## 기능 수정

- Railway/Streamlit 환경의 한글 PDF 폰트 경로 오류 완화
- 크레탑 고객등록과 정책자금 키워드 저장 단계 분리
- 정책자금 키워드 정규화 및 안전 저장
- 주요 오류 자동 로그 기록
- 외부 라이브러리 Import 없이 문법검사만 수행

## 업데이트 엔진 수정

- 이전 패치폴더를 삭제하지 않음
- 이전 패치 ZIP을 삭제하지 않음
- 이전 BAT 및 업데이트 엔진을 삭제하지 않음
- 현재 패치폴더를 업데이트 후에도 보존
- 명시된 프로젝트 코드 파일만 백업 후 덮어쓰기
- 실패 시 이번 패치가 관리하는 대상 파일만 자동 복원
