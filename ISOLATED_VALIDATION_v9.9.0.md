# OASIS CRM v9.9.0 격리 Supabase 검증 기록

- 검증일: 2026-08-03 (KST)
- 검증 대상: `supabase/migrations/20260803080418_v990_data_safety.sql`
- 마이그레이션 SHA-256: `D8984C6A7E328A760057B99FB1BFD92365FEC02944850E16607A3C1D5A915E9D`
- 격리 브랜치: `v990-isolation-final`
- 운영 Supabase 변경: 없음
- GitHub push / Railway 배포: 없음
- 사용 데이터: 식별 불가능한 고정 합성 데이터만 사용

## 1. 판정

격리 브랜치에서 최신 마이그레이션을 깨끗한 합성 기준 데이터 위에 연속 두 번 적용했다. 이후 최종 감사에서 발견한 오류 마스킹·동시 멱등성·복합 외래키 인덱스를 보완한 최종 SQL도 다시 연속 두 번 적용했다. 모든 실행이 오류 없이 끝났고, 마지막 실행 뒤에도 테이블·트리거·함수·버킷이 중복 생성되지 않았다.

합성 데이터의 논리 백업·삭제·복원 훈련과 대기열·고객 아카이브 RPC의 원자성 및 멱등성 시험도 통과했다. 운영 적용을 막는 데이터 유실 또는 권한 노출 결함은 이 범위에서 발견되지 않았다.

## 2. 마이그레이션 2회 적용 결과

| 검사항목 | 결과 |
|---|---:|
| 합성 고객 | 2건 유지 |
| 합성 녹취 메타데이터 | 1건 유지 |
| 고객 기준 해시 | `0c3d0fb1852f9a592ab9430eba45291c` 유지 |
| 녹취 메타데이터 해시 | `41418f2711b78ad2b25167a70683e967` 유지 |
| 신규 테이블 | 11개 |
| FORCE RLS 적용 | 11개 |
| 제한 RPC 함수 | 9개 |
| 고정 `search_path` 함수 | 9개 |
| 갱신 트리거 | 11개, 테이블당 최대 1개 |
| 고객 보존 컬럼 | 6개 |
| 비공개 Storage 버킷 | 1개 |
| 미검증 제약조건 | 0개 |
| 무효·준비 안 된 인덱스 | 0개 |
| 복합 외래키 보조 인덱스 | 10개 |
| `anon`/`authenticated` 테이블 권한 | 0개 |
| `anon`/`authenticated` RPC 실행권한 | 0개 |
| 기존 고객·녹취 테이블 RLS | 2/2 활성화 |
| 기존 고객·녹취 브라우저 직접 권한 | 0개 |
| `service_role` DELETE 권한 | 0개 |

## 3. 동적 RPC 검증

### 영속 동기화 대기열

- 같은 소유자와 멱등 키로 완전히 같은 요청을 다시 보내면 같은 작업 행을 반환한다.
- 같은 멱등 키를 다른 작업·대상·payload에 재사용하면 `OASIS_OUTBOX_IDEMPOTENCY_CONFLICT`로 거절한다.
- `pending → processing → retry → pending → processing → completed` 전이가 정상 동작한다.
- 임대 토큰이 일치하는 작업만 완료·실패 처리한다.
- 수동 재처리 횟수는 1회 증가하고 실제 시도 이력은 2회로 보존된다.
- 오류 요약의 합성 전화번호·이메일·경로가 각각 전용 마커로 치환된다.
- 공백이 포함된 합성 고객명·주소와 `Authorization: Bearer ...` 전체가 `[REDACTED]`로 치환되어 뒤쪽 토큰이 남지 않는다.
- 모든 동적 시험은 트랜잭션 롤백 후 outbox 및 이벤트 잔여 행이 0건임을 확인했다.

### 고객 아카이브·재활성화

- 동일한 행위자·사유·멱등 키의 재요청은 동일 작업으로 인정한다.
- 같은 멱등 키를 다른 행위자 또는 다른 사유로 재사용하면 각각 전용 충돌 오류로 거절한다.
- 동일 고객·동일 키 동시 요청은 이벤트가 1건만 생성되고, 서로 다른 고객 또는 서로 다른 동작이 같은 키를 경쟁하면 패자 행의 상태가 바뀌지 않는다.
- 이미 목표 상태인 고객에 새 키를 사용하면 `state_changed=false` 감사 이벤트 1건을 남기며, 같은 키의 상충 재사용은 거절한다.
- 아카이브와 재활성화가 하나의 트랜잭션 안에서 고객 상태와 감사 이벤트를 함께 갱신한다.
- 시험 롤백 후 합성 고객 상태·감사 이벤트·기준 데이터 해시가 원래 값으로 복원됐다.

## 4. 백업·복구 훈련

격리 브랜치에서 합성 고객 2건과 녹취 메타데이터 1건의 논리 스냅샷을 만들고, 원본 행을 비운 뒤 스냅샷으로 복원했다. 복원 후 두 대상의 건수와 해시가 모두 기준값과 일치했고, 비공개 버킷 메타데이터도 유지됐다. 전체 훈련은 트랜잭션 안에서 수행하고 마지막에 롤백하여 시험 잔여 데이터를 남기지 않았다.

별도의 신규 복구 브랜치 생성도 시도했으나, Supabase Branching은 운영의 현재 스키마가 아니라 저장소의 마이그레이션 이력으로 새 DB를 재구성한다. 현재 저장소에는 운영 기본 스키마 전체를 만드는 baseline migration이 없고 v9.9.0 증분 마이그레이션만 있으므로, 빈 브랜치는 필수 기존 테이블을 찾지 못해 자동 구성에 실패했다. 이는 v9.9.0 증분 SQL의 멱등성 실패가 아니라 저장소에 운영 baseline migration이 없는 배포 구조상의 제한이다.

따라서 이번에 통과한 범위는 다음과 같다.

- 격리 DB의 증분 마이그레이션 최초 적용 및 재적용
- 합성 Database 행의 논리 백업·삭제·복원
- 스키마·RLS·권한·RPC·버킷 메타데이터 검증

다음 항목은 아직 운영 승인 이후 별도 실행기가 필요한 범위다.

- 실제 운영 Database 물리 백업 파일 복원
- Supabase Auth 설정 및 사용자 복원
- 실제 Storage 객체 바이트 백업·복원과 체크섬 대조

## 5. Advisor 결과 해석

- 보안 Advisor의 `RLS enabled no policy`는 신규 내부 테이블을 브라우저 역할에서 완전히 잠그고 `service_role` 및 제한 RPC만 사용하도록 만든 의도된 서비스 전용 설계다. 별도 검증에서도 브라우저 테이블 권한과 RPC 실행권한이 모두 0개임을 확인했다.
- 성능 Advisor가 처음 보고한 복합 외래키 covering index 10건은 최종 마이그레이션에 보완했고 재검사에서 모두 해소됐다.
- 남은 미사용 인덱스 INFO는 데이터가 거의 없는 새 격리 브랜치 특성상 정상이며, Auth 연결 수 설정 INFO는 프로젝트 운영 설정 항목이라 이번 증분 SQL 범위가 아니다.

## 6. 로컬 회귀검사

- 1단계 지정 테스트: `115 passed`
- 전체 테스트: `541 passed`, `1 failed`, `1 skipped`, `21 subtests passed`
- 전체 테스트 실패 1건은 변경하지 않은 `claim_correction_center.py`에 원래 존재하는 `st.rerun()` 4개와 테스트 기대값 2개의 차이다. 이번 마이그레이션·오류 마스킹·동기화 대기열 변경과 무관하며, 경정청구 금지 범위를 임의 수정하지 않았다.
- `git diff --check`: 통과

## 7. 비용과 정리

격리 브랜치 표시 단가는 시간당 약 `$0.01344`였다. 정확한 청구액은 Supabase 청구서에서 확정되지만, 이번 실행 시간 기준 예상 비용은 `$0.05` 미만이다. 검증 종료 후 격리 브랜치를 삭제하여 추가 과금을 중단한다.

## 8. 남은 승인 경계

이 검증은 운영 변경 승인이 아니다. 다음 작업은 사용자 별도 승인 전까지 수행하지 않는다.

1. 운영 Supabase 마이그레이션 적용
2. 신규 기능 플래그의 순차 활성화
3. GitHub commit/push
4. Railway 배포
