# OASIS CRM v9.11.0 고객정보 통합

## 변경 내용

- 로컬 누적 고객DB와 Supabase 고객을 함께 읽어 어느 한쪽이 비어도 고객 목록이 사라지지 않게 했다.
- 자동 통합은 소유자가 같고 정규화된 10자리 사업자등록번호가 정확히 하나씩 일치할 때만 수행한다. 이름·주소만으로는 통합하지 않는다.
- 로컬의 비어 있지 않은 값을 우선하고 클라우드는 빈 값을 보완한다. 중복 후보와 식별 불가 행은 합치지 않고 모두 보존한다.
- 기존 고객 UUID와 기존 영업 `company_uid` 사이에 불변 교차 연결을 추가했다. 어느 식별자도 재발급하거나 다시 쓰지 않는다.
- CRM, 재무, 등기, 매칭설정, 고객이력, 상담일지, 고객 휴지통, 주가평가에 nullable `customer_id` FK와 인덱스를 추가했다.
- 과거 행은 소유자 범위의 단일 정확 일치만 연결하고, 이후 행은 같은 규칙의 트리거로 연결한다. 기존의 비어 있지 않은 링크는 덮어쓰지 않는다.
- 고객 프로필 저장은 빈 값이 기존 필드를 지우지 않는 무손실 JSON 병합 RPC를 사용한다.
- RPC 부재·충돌·일시 오류 시 직접 테이블 upsert로 폴백하지 않고, 허용 목록과 소유자 검사를 통과한 RPC 작업만 재시도 대기열에 보존한다.
- 보관 상태 고객은 데이터에서는 유지하되 활성 고객 선택 목록에서는 제외한다. 활성 고객이 하나도 없어도 UI가 중단되지 않는다.
- 고객 이관 결과의 식별 불가·대기·실패 건은 완료로 집계하지 않으며, 오류 원문 대신 정제된 유형만 이력에 저장한다.

## Supabase

- 마이그레이션: `supabase/migrations/20260804020000_v911_customer_information_integration.sql`
- SHA-256: `206B7A4C2116D1F599EBA45DEDF106FDE56CD407BBF0B39ABCC4BC064DB3136C`
- 신규 테이블: `oasis_customer_company_links`, `oasis_customer_identity_reviews`
- 신규 앱 RPC: `oasis_upsert_customer_profile`, `oasis_list_unified_customers`
- 신규 테이블은 RLS와 FORCE RLS를 적용하고 `PUBLIC`, `anon`, `authenticated` 직접 접근을 차단했다.
- 앱 RPC는 `service_role`만 실행할 수 있고 고정 `search_path`를 사용한다.

## 비파괴 검증

- 격리 브랜치에서 동일 마이그레이션을 연속 2회 적용했다.
- 합성 운영형 스키마에서 기존 11개 테이블의 행 수와 고객 UUID·`company_uid` 지문이 두 적용 후에도 동일했다.
- 8개 연관 테이블의 과거 행 backfill과 미래 행 트리거, 소유자 격리, 충돌 거부, 무손실 병합을 검증했다.
- 13개 관련 테이블의 논리 백업→복원 행 수와 전체 행 지문이 일치했다.
- 운영 적용 전후 고객/CRM 관련 11개 테이블의 행 수와 안정 식별자 지문이 모두 동일했다.
- 전화수집·자격수집·NPS·원천DB·대기열 테이블에서 행 감소가 없었다. 전화수집 상태 지문 변화는 이번 SQL과 무관한 실행 중 백그라운드 작업으로 확인했다.
- 운영 Supabase 보안·성능 어드바이저 오류/경고는 0건이다. 신규 미사용 인덱스와 직접 접근을 닫은 RLS 무정책 표시는 정보 수준이다.
- 전체 테스트: `652 passed, 1 skipped, 22 subtests passed`
- Python 구문 검사, `git diff --check`, Privacy Guard 통과

## 보호 범위

- 고객 데이터 물리 삭제 없음
- 기존 고객 UUID 및 `company_uid` 변경 없음
- 전화번호 자동수집·원천DB 대기열 재생성 또는 상태 덮어쓰기 없음
- 경정청구 자료수집 API, 문서 종류, 인증 절차, Tilko 호출 로직 변경 없음
- Storage 원본 덮어쓰기 없음

## 배포 상태

- 운영 Supabase 마이그레이션: 완료
- GitHub `main`: 반영 대기
- Railway: 배포 검증 대기
- 위 두 항목 검증 전에는 3단계를 시작하지 않는다.
