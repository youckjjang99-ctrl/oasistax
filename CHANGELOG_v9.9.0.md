# OASIS CRM v9.9.0-data-safety 변경이력

## Added

- 재배포 후에도 유지할 수 있는 Supabase 영속 outbox 스키마와 원자적 RPC 초안
- 로컬 fallback 대기열의 멱등 키, 지수형 재시도, dead-letter, 완료 이력
- 고객 문서용 비공개 Storage 메타데이터와 짧은 유효기간 signed URL 경로
- 하나의 비공개 Storage 원본을 여러 고객·출처에 중복 업로드 없이 연결하는
  `oasis_customer_asset_links` 메타데이터
- AI 코파일럿 메모·성공사례·체크리스트의 선택적 클라우드 이중 저장
- 백업 실행·격리 복구시험·고객 아카이브 이벤트 메타데이터
- 고객정보를 물리 삭제하지 않는 원자적 아카이브·재활성화 RPC와 관리자 작업 경로
- 관리자용 데이터 안전성·전사 영속 재시도 대기·백업·복구시험 상태 화면
- 신규 변경 파일 개인정보·비밀키 유입을 차단하는 CI Privacy Guard
- 인증서·로컬 DB 확장자 차단과 승인된 공개 데이터의 exact allowlist
- `.env.example`, Markdown, SQL을 포함한 텍스트 개인정보·비밀값 검사
- Git 비교·커밋·스테이징 검사는 새로 추가된 줄만 내용 검사하고, 바이너리와
  민감 파일명은 계속 전체 경로 기준으로 차단하도록 legacy baseline 호환성 보완
- 스테이징 전 HEAD 대비 변경분을 확인하는 `--working-tree` 검사 모드
- 오류 로그의 주민번호·전화번호·사업자번호·이메일·고객명·주소·메모·인증정보·토큰 마스킹
- 공백이 포함된 고객명·주소와 `Authorization: Bearer ...` 전체를 남김없이 제거하는 Python·SQL 오류 마스킹
- 공개 오류 메시지에서 원문 예외 메시지와 로컬 전체 경로를 제거하는 공용 helper
- 정책자금 매칭 엔진의 사용자별 격리 실행 경로

## Changed

- 로컬 동기화 대기열의 500건 절단을 제거하고 원자적 파일 교체로 저장
- 성공한 동기화 항목도 감사 가능한 완료 상태로 보존
- 고객 이력 로컬 캐시 제한과 클라우드 장기 원본 저장을 분리
- 상담 녹취 물리 삭제 경로를 차단하고 기존 삭제 동작을 비파괴 아카이브로 전환
- 고객·경정청구 업로드 원본의 메타데이터 저장 실패 또는 새 버전 등록 시에도
  기존 Storage 원본을 자동 삭제하지 않도록 보존
- 고객 문서가 Storage에 올라간 뒤 메타데이터 또는 고객 연결 저장이 실패하면
  원본과 업로드 객체를 유지한 채 멱등 복구 작업을 영속/로컬 대기열에 등록
- 동명 결과 파일을 덮어쓰거나 삭제하지 않고 충돌 없는 이름으로 함께 보존
- 업로드 원본과 매칭 결과의 비공개 클라우드 복사를 기능 플래그 뒤에 추가
- 고객 보관·재활성화 RPC의 동일 멱등 키 동시 요청을 트랜잭션 advisory lock으로 직렬화
- 이미 목표 상태인 고객의 새 멱등 키도 `state_changed=false` 감사 이벤트로 보존
- 소유자 범위 복합 외래키 10개에 covering index 추가

## Security

- 신규 public 테이블 전체에 RLS/FORCE RLS 적용
- PUBLIC, anon, authenticated 직접 접근 차단
- service_role에 필요한 SELECT/INSERT/UPDATE와 제한 RPC만 명시적으로 부여
- SECURITY DEFINER RPC의 search_path 고정 및 실행 권한 최소화
- Supabase 기본 ACL로 부여될 수 있는 `service_role` 광범위 테이블 권한을 먼저 전부 회수한 뒤 필요한 최소 권한만 재부여
- 고객 문서 버킷을 비공개로 유지하고 공개 URL 생성을 사용하지 않음
- 관리자 화면에서 비밀키 조각, 원문 예외, 마이그레이션 오류 원문을 표시하지 않음

## Isolated validation

- 격리 Supabase에서 최신 데이터 안전성 마이그레이션을 연속 두 번 적용해 멱등성 확인
- 합성 고객 2건과 녹취 메타데이터 1건의 기준 해시 및 건수 보존 확인
- 신규 테이블 11개와 제한 RPC 9개의 RLS, 고정 `search_path`, 최소권한 검증
- outbox 멱등 키 충돌, 임대·실패·수동 재처리·완료 전이 및 오류 요약 마스킹 검증
- 고객 아카이브·재활성화 멱등 키의 행위자·사유 충돌 방지 검증
- 두 세션 동시 배정과 보관·재활성화 교차동작에서 이벤트 1건 및 패자 상태 불변 검증
- 공백 포함 합성 고객명·주소와 Bearer 자격증명 마스킹의 실제 SQL 실행 검증
- Supabase Advisor의 복합 외래키 인덱스 권고 10건 해소 확인
- 합성 Database 행의 논리 백업·삭제·복원 훈련 완료

## Known limitations

- CI Privacy Guard는 PR과 push로 GitHub에 들어온 변경을 검사한다. `main` 브랜치에
  required status check와 branch protection이 적용되어야 미검사 병합을 차단할 수 있다.
  직접 push 자체는 GitHub 수신 이후 검사되므로, 로컬 단계 유입을 선제 차단하려면
  동일 검사를 pre-commit 또는 staged 검사로 추가 운영해야 한다.
- 이번 패치는 백업·복구 증거 스키마와 읽기 전용 표시를 제공한다. 실제 Database,
  Auth 설정, Storage 객체 백업 실행기와 주기, RPO/RTO, 보존기간은 운영 승인 후 확정한다.
- 동일 파일의 다중 고객·출처 연결 어댑터와 스키마는 격리 PostgreSQL에서 최초 실행과
  재실행을 검증했다. 기존 운영 자산 링크 이관과 동시 업로드 검증은 아직 수행하지
  않았으므로 해당 시험을 마친 뒤 비공개 자산 기능 플래그를 활성화한다.
- 저장소에 운영 전체 스키마를 만드는 baseline migration이 없어 빈 preview branch가
  증분 마이그레이션만으로 자동 복구되지 않는다. 합성 데이터의 논리 복구는 통과했지만
  실제 Database/Auth/Storage 물리 복구는 별도 운영 절차와 실행기가 필요하다.
- 동일 체크섬 파일을 서로 다른 작업이 동시에 최초 업로드하면 물리 객체가 둘 이상
  만들어질 가능성은 남아 있다. 고객 연결 충돌과 데이터 유실은 방지하지만, 운영 전
  고아 객체 재조정 작업과 동시성 시험이 필요하다.

## Preserved

- 기존 고객·CRM·상담·재무·문서·AI 자료와 고객 ID
- 기존 로컬 파일과 Git 이력
- 기존 `고객DB.xlsx` 추적 상태와 파일 내용
- DB발굴, 기업등록, 기업컨설팅, 경정청구, Tilko 인증·수집 로직

## Deployment status

- 운영 Supabase 마이그레이션: 미적용
- GitHub push: 미수행
- Railway 배포: 미수행
- 신규 기능 플래그: 기본 OFF
