# OASIS CRM v9.9.0 데이터 백업·복구 설계

## 원칙

- 고객 원본정보와 변경이력은 자동 삭제하지 않는다.
- 데이터베이스, Auth, Storage 객체, 녹취, 자동화 상태를 서로 다른 백업 대상으로 관리한다.
- 운영 복구시험은 운영 프로젝트가 아니라 접근이 제한된 격리 프로젝트에서 수행한다.
- 백업 파일 경로, 인증정보, 고객 식별정보는 일반 화면과 로그에 출력하지 않는다.
- 백업 완료는 파일 생성만이 아니라 체크섬 검증과 대상별 건수 기록까지 성공한 경우로 한정한다.

## 백업 대상

1. Supabase Database: 고객, CRM, 상담, 영업, 자동화, 감사·변경이력
2. Supabase Auth: 사용자 ID, 역할, 계정상태 및 복구에 필요한 설정
3. Supabase Storage: 고객 문서, 분석 결과, 상담 녹취, AI 자료
4. 운영 설정: Railway 변수의 이름 목록과 필수 설정 존재 여부(값 자체는 별도 비밀 저장소)
5. 작업 상태: 영속 outbox, dead-letter, 스케줄 작업 실행 상태

Supabase Database 백업은 Storage 객체 파일을 포함하지 않는다. Database 백업과
Storage 객체 백업은 별도 작업으로 실행하고, 두 결과의 체크섬·객체 수·누락 대상을
각각 검증해야 한다. Auth 스키마의 사용자 레코드는 Database 복구 범위에 포함될 수
있지만 Auth 공급자 설정, 리디렉션 URL, SMTP 설정, API 키와 프로젝트 설정은 자동으로
동일 복원된다고 간주하지 않는다. 격리 복구시험에서 별도 설정 및 재발급 여부를 확인한다.

## 백업 기록

`oasis_backup_runs`에는 백업 유형, 상태, 시작·완료시각, 보존기한, 체크섬,
파일 크기, 대상별 레코드 건수와 안전한 오류코드만 기록한다. 실제 문서는 비공개
Storage 또는 별도 암호화 백업 저장소에 둔다.

## 복구시험

1. 최신 검증 완료 백업을 선택한다.
2. 운영과 분리된 격리 Supabase 프로젝트를 준비한다.
3. 스키마와 데이터를 복구하고 Storage 객체를 별도로 복구한다.
4. 고객·상담·연락·문서·감사로그 건수와 참조 무결성을 비교한다.
5. 무작위 표본 문서의 체크섬과 signed URL 접근권한을 검증한다.
6. 일반 사용자가 다른 담당자의 자료를 볼 수 없는지 RLS 회귀 테스트를 실행한다.
7. 결과를 `oasis_restore_drills`에 기록하고 격리 환경을 폐쇄한다.

## 2026-08-03 격리 훈련 결과

- 격리 Supabase preview branch `v990-isolation-final`에서 합성 고객 2건과 녹취 메타데이터 1건으로 수행했다.
- 최종 증분 마이그레이션(SHA-256 `D8984C6A7E328A760057B99FB1BFD92365FEC02944850E16607A3C1D5A915E9D`)을 연속 두 번 적용했고 두 실행 모두 성공했다.
- 합성 Database 행의 논리 스냅샷을 만든 뒤 원본 행을 비우고 다시 복원했다.
- 복원 뒤 고객 2건과 녹취 메타데이터 1건이 유지됐고, 고객 해시
  `0c3d0fb1852f9a592ab9430eba45291c`와 녹취 메타데이터 해시
  `41418f2711b78ad2b25167a70683e967`가 각각 원본과 일치했다.
- RLS·권한·RPC·인덱스·제약조건·비공개 버킷 메타데이터 검사를 통과했다.
- 시험 변경은 모두 트랜잭션 롤백했고 outbox·감사 이벤트 잔여 행이 0건임을 확인했다.
- 운영 Database/Auth/Storage 객체는 사용하거나 변경하지 않았다.

이 훈련은 합성 Database 행의 논리 복구 검증이다. 실제 운영 Database 물리 백업 파일,
Auth 공급자 설정, Storage 객체 바이트와 체크섬 복구를 대신하지 않는다. 또한 저장소에
운영 전체 스키마를 만드는 baseline migration이 없어 빈 preview branch 자동 구성은
실패하므로, 운영 복구 체계를 완성하려면 baseline 정비와 전용 백업 실행기가 필요하다.
상세 증거는 `ISOLATED_VALIDATION_v9.9.0.md`에 기록한다.

## 운영 적용 전 승인 항목

- Supabase 마이그레이션 실행
- 비공개 Storage 버킷 생성 확인
- 백업 저장소와 보존기간 결정
- 기능 플래그 `OASIS_DATA_SAFETY_V1`, `OASIS_DURABLE_OUTBOX_V1`,
  `OASIS_PRIVATE_ASSETS_V1`, `OASIS_CLOUD_COPILOT_V1` 활성화
- 최초 운영 백업 및 Database/Auth/Storage 물리 복구시험 실행
- 목표 RPO·RTO, 백업 주기와 보존기간 확정
- Auth 설정·키 재구성 절차와 Storage 객체 백업 실행기 확정

현재 1단계 패치에는 스키마와 읽기 전용 운영 화면, 안전한 저장 경로가 포함되며,
운영 백업 실행이나 운영 데이터 복구는 승인 없이 자동 수행하지 않는다.
관리자 화면의 실행 기록은 백업 실행기가 체크섬, 대상별 건수, 누락 대상과 복구시험
결과를 실제로 기록한 경우에만 증거가 된다. 메타데이터 테이블이나 화면이 존재한다는
사실만으로 백업 또는 복구 가능성이 검증된 것은 아니다.
